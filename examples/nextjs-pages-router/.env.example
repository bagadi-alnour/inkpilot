# Copy to .env.local and fill in at least one key to enable AI features.

# OpenAI API key (sk-...)
NEXT_PUBLIC_OPENAI_KEY=

# Anthropic API key (sk-ant-...)
NEXT_PUBLIC_ANTHROPIC_KEY=

# Optional: force provider when both keys are set — "openai" or "anthropic"
NEXT_PUBLIC_AI_PROVIDER=
